import QtQuick

import Style 1.0

CopyableTextType {
    lineHeight: 24
    lineHeightMode: Text.FixedHeight

    color: AmneziaStyle.color.paleGray
    font.pixelSize: 16
    font.weight: 600
    font.family: "Inter"

    wrapMode: Text.WordWrap
}
